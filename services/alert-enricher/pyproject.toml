[project]
name = "alert-enricher"
version = "0.1.0"
description = "Enriquece las alertas de Alertmanager con el trace_id del exemplar"
requires-python = "==3.12.*"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
]

[tool.uv]
package = false
